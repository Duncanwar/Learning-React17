"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.pluralify = exports.arrayIn = exports.camelCase = exports.capitalize = void 0;
const is_1 = require("../is");
const utils_1 = require("../utils");
function capitalize(str) {
    if (!(0, is_1.isString)(str)) {
        return '';
    }
    return str.charAt(0).toUpperCase() + str.slice(1);
}
exports.capitalize = capitalize;
function camelCase(str) {
    return (0, utils_1.words)(str.replace(/['\u2019]/g, '')).reduce((result, word, index) => {
        word = word.toLowerCase();
        return result + (index ? capitalize(word) : word);
    }, '');
}
exports.camelCase = camelCase;
function arrayIn(arr, str) {
    let found = false;
    arr.forEach((item) => {
        if (str.includes(item)) {
            found = true;
        }
    });
    return found;
}
exports.arrayIn = arrayIn;
function pluralify(singular, plural, count) {
    return count === 1 ? singular : plural;
}
exports.pluralify = pluralify;
